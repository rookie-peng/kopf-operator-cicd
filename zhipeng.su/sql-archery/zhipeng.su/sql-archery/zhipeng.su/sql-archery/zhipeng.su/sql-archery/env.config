[app-setting]
DEBUG = True
mysql = 1.1.1.1:3306
app = archery


[global-config]
image = nginx:latest  # 有该选项会默认不进行构建, 而直接采用该镜像， 可不填写
dockerfile = False    # True or False, 是否提供自定义的Dockerfile, 默认为False, 可不填写
ingress = xxx.com     # 设置服务的ingress域名, 可不填写